---
import { getLangFromUrl, useTranslations, getLocalizedPath } from '@shared/lib/i18n';

const lang = getLangFromUrl(new URL(Astro.request.url));
const t = useTranslations(lang);

const currentYear = new Date().getFullYear();
---

<footer
	class="opacity-80 px-4 sm:px-6 lg:px-8 mt-12 sm:mt-16 mx-auto mb-4 sm:mb-6 flex justify-center"
>
	<div
		class="rounded-lg mx-auto flex flex-col sm:flex-row items-center justify-between gap-4 py-4"
	>
		<span class="text-sm sm:text-center text-zinc-800/90 dark:text-zinc-200/90"
			>© {currentYear}
			<a href="https://github.com/dev-sandoval" class="hover:underline">David Sandoval</a>. {
				t('footer.rights')
			}
		</span>
		<ul
			class="flex flex-wrap items-center justify-center mt-3 text-sm font-medium dark:text-white/90 sm:mt-0"
		>
			<a href={getLocalizedPath(lang, '/#about-me')} class="hover:underline me-4 md:me-6"
				>{t('footer.about')}</a
			>
			<a id="contacto" href="mailto:sandovaldavid2201@gmail.com" class="hover:underline"
				>{t('footer.contact')}</a
			>
		</ul>
	</div>
</footer>
